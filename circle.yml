machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker
dependencies:
  pre:
    - rm /opt/circleci/php/$(phpenv global)/etc/conf.d/xdebug.ini
  override:
    - sudo pip install docker-compose
    - docker-compose up -d mariadb
    - docker-compose up -d nginx
test:
  pre:
    - composer global require "hirak/prestissimo:^0.3"
    - curl https://github.com/pantheon-systems/terminus/releases/download/0.x/terminus.phar -L -o terminus.phar && chmod +x terminus.phar
    - php terminus.phar auth login --machine-token=$TERMINUS_TOKEN
    - php terminus.phar sites aliases
    - composer install --prefer-dist
    - sudo chown -R 82:82 .
  override:
    - docker-compose run --user 82 php vendor/bin/robo ci:test
