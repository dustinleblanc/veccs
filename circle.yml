machine:
  php:
    version: 7.0.4
database:
  override:
    - cd web && ../vendor/bin/drush si -y
test:
  pre:
    - ../vendor/bin/drush rs 8889 -y
  override:
    - cd .. && php vendor/codegyre/robo/robo test

deployment:
  dev:
    branch: master
    commands:
     - php vendor/codegyre/robo/robo deploy
