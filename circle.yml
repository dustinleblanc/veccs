machine:
  php:
    version: 7.0.4
database:
  override:
    - mysql < tests/_data/dump.sql
test:
  pre:
    - cp web/sites/example.setting.local.php web/sites/default/settings.local.php
    - cd web && ../vendor/bin/drush rs 8889 -y
  override:
    - cd .. && php vendor/codegyre/robo/robo test

deployment:
  dev:
    branch: master
    commands:
     - php vendor/codegyre/robo/robo deploy
