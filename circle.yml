machine:
  php:
    version: 7.0.4
database:
  override:
    - ./vendor/bin/drush si --db-url=mysql://ubuntu@localhost/circle_test
test:
  pre:
    - ./vendor/bin/drush rs 8889
  override:
    - php vendor/codegyre/robo/robo test

deployment:
  dev:
    branch: master
    commands:
     - php vendor/codegyre/robo/robo deploy
